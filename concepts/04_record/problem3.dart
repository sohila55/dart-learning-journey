

/*
Problem 3 — Word Frequency, Top Result
Difficulty: Medium
Given a string of words, find the most frequent word and return (String word, int count). If there's a tie, return the one that appears first alphabetically.
Input: "the cat sat on the mat the cat ran"
Output: (word: "the", count: 3)
Must use: a typedef to name the record shape (e.g. typedef WordStat = (String word, int count);), and use it as the function's return type.

*/